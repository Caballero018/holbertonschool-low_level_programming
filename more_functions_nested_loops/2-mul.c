#include "main.h"

/**
 * mul - Function that multiplies 2 integers.
 *
 * @a: One of the integers to multiply.
 * @b: The other integer to multiply.
 * Return: Returns the multiplication of two variables.
 */
int mul(int a, int b)
{
	return (a * b);
}
