#include<stdio.h>

/**
 * main - One digit of base 10
 *
 * Return: 0 always
 */

int main(void)
{
        int x;

        for (x = 0 ; x < 10 ; x++)
        {
                putchar(x);
        }

        putchar('\n');

        return (0);
}
