#include "main.h"

/**
 * _strlen - returns the length of a string.
 *
 * @s: Variable that point char.
 * Return: point
 *
 *
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; i < '\0' ;i++)
	{
		return (s);
	}
}
