#include "main.h"

/**
 * print_alphabet_x10 - the alphabet multiplicate * 10
 *
 * Return: 0
 *
 */
void print_alphabet_x10(void)
{
	char x = 'a';

	while (x <= 'z')
	{
		if(x == a)
		{
			_putchar(x * 10);
			x++;			
		}
	}
}
